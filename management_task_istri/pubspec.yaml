name: management_task_istri
description: "A new Flutter project."

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.2.0
  firebase_auth: ^5.1.1
  google_sign_in: ^6.2.1

  # Backend & Database (Supabase)
  supabase_flutter: ^2.6.0

  # State Management (BLoC)
  flutter_bloc: ^8.1.3
  hydrated_bloc: ^9.1.3
  path_provider: ^2.1.3

  # Device Integration (Kamera, Galeri, & Notifikasi)
  image_picker: ^1.1.2
  image_cropper: ^5.0.1
  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.4

  # UI Components & Utilities
  fl_chart: ^0.69.0
  equatable: ^2.0.5
  uuid: ^4.3.3

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
